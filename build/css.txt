:root{
  --bg:#0a0b10;
  --bg2:#14161f;
  --bg3:#1c1f2b;
  --ink:#f4ecdd;
  --muted:#b7b0a4;
  --accent:#2fe6ff;
  --accent-rgb:47,230,255;
  --accent2:#ff2fb0;
  --accent2-rgb:255,47,176;
  --good:#5cf29a;
  --good-rgb:92,242,154;
  --bad:#ff5566;
  --warn:#ffd93d;
}
html,body{background:var(--bg)!important;}
tw-story{
  background:var(--bg);
  color:var(--ink);
  font-family:'Segoe UI',system-ui,-apple-system,Roboto,sans-serif;
  line-height:1.5;
  max-width:720px;
  margin:0 auto;
  padding:0 0 3em 0;
}
tw-sidebar{display:none;}
tw-passage{
  display:block;
  padding:1.4em 1.1em 2.2em 1.1em;
  animation:fadein .35s ease-out;
}
@keyframes fadein{from{opacity:0;transform:translateY(6px);}to{opacity:1;transform:translateY(0);}}
tw-passage h1,tw-passage h2,tw-passage h3{
  color:var(--accent);
  text-shadow:0 0 14px rgba(var(--accent-rgb),0.55);
  letter-spacing:.02em;
  line-height:1.25;
  margin:.2em 0 .5em 0;
}
tw-passage h1{font-size:1.5em;}
tw-passage h2{font-size:1.2em;color:var(--accent2);text-shadow:0 0 12px rgba(var(--accent2-rgb),0.5);}
tw-passage p{margin:.7em 0;}
tw-passage hr{
  border:none;
  height:1px;
  margin:1.4em 0;
  background:linear-gradient(90deg,transparent,var(--accent),transparent);
  opacity:.5;
}
tw-passage em{color:var(--muted);}
tw-passage strong{color:var(--ink);}
tw-link, a.link{
  display:block;
  box-sizing:border-box;
  width:100%;
  min-height:48px;
  padding:.85em 1em;
  margin:.5em 0;
  border:1.5px solid var(--accent);
  border-radius:12px;
  color:var(--ink)!important;
  background:linear-gradient(180deg, rgba(var(--accent-rgb),0.16), rgba(var(--accent-rgb),0.05));
  text-decoration:none!important;
  font-weight:600;
  font-size:1rem;
  cursor:pointer;
  transition:transform .12s ease, box-shadow .12s ease, background .12s ease;
}
tw-link:hover, tw-link:active, tw-link:focus{
  background:linear-gradient(180deg, rgba(var(--accent-rgb),0.32), rgba(var(--accent-rgb),0.1));
  box-shadow:0 0 18px rgba(var(--accent-rgb),0.45);
  transform:translateY(-1px);
}
tw-broken-link{
  display:block;
  padding:.85em 1em;
  margin:.5em 0;
  border:1.5px dashed var(--bad);
  border-radius:12px;
  color:var(--bad)!important;
}
tw-passage textarea, tw-passage input[type=text]{
  display:block;
  width:100%;
  box-sizing:border-box;
  background:var(--bg3);
  color:var(--ink);
  border:1.5px solid var(--muted);
  border-radius:10px;
  padding:.7em .8em;
  font-size:1rem;
  font-family:inherit;
  margin:.4em 0 1em 0;
}
tw-passage textarea:focus, tw-passage input[type=text]:focus{
  outline:none;
  border-color:var(--accent);
  box-shadow:0 0 10px rgba(var(--accent-rgb),0.4);
}
.tag{
  display:inline-block;
  font-size:.72em;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:var(--bg);
  background:var(--accent);
  padding:.25em .6em;
  border-radius:999px;
  margin-bottom:.6em;
  font-weight:700;
}
.pron{color:var(--accent2);font-style:italic;margin:.1em 0 .6em 0;}
.card{
  background:var(--bg2);
  border:1px solid rgba(var(--accent-rgb),0.35);
  border-radius:14px;
  padding:1em 1.1em;
  margin:.8em 0;
}
.vs{
  text-align:center;
  font-weight:800;
  color:var(--warn);
  margin:.6em 0;
  letter-spacing:.08em;
}
.stage-namepit{--accent:#ff2fb0;--accent-rgb:255,47,176;--accent2:#2fe6ff;--accent2-rgb:47,230,255;}
.stage-knockout{--accent:#ffd93d;--accent-rgb:255,217,61;--accent2:#ff2fb0;--accent2-rgb:255,47,176;}
.stage-finalvote{--accent:#5cf29a;--accent-rgb:92,242,154;--accent2:#2fe6ff;--accent2-rgb:47,230,255;}
.stage-result{--accent:#5cf29a;--accent-rgb:92,242,154;--accent2:#ffd93d;--accent2-rgb:255,217,61;}
.copybtn{
  display:block;
  width:100%;
  min-height:48px;
  padding:.85em 1em;
  margin:.6em 0;
  border:1.5px solid var(--good);
  border-radius:12px;
  background:rgba(var(--good-rgb),0.12);
  color:var(--good);
  font-weight:700;
  font-size:1rem;
  font-family:inherit;
  cursor:pointer;
}
.copybtn:active{background:rgba(var(--good-rgb),0.25);}
.resultbox{
  width:100%;
  box-sizing:border-box;
  min-height:7em;
  background:var(--bg3);
  color:var(--ink);
  border:1.5px solid var(--good);
  border-radius:10px;
  padding:.8em;
  font-family:inherit;
  font-size:.95rem;
}
@media (max-width:420px){
  tw-passage{padding:1.1em .8em 2em .8em;}
  tw-passage h1{font-size:1.3em;}
}
