:root{
  --bg:#0a0b10;
  --bg2:#14161f;
  --bg3:#1c1f2b;
  --ink:#f4ecdd;
  --muted:#b7b0a4;
  --accent:#2fe6ff;
  --accent-rgb:47,230,255;
  --accent2:#ff2fb0;
  --accent2-rgb:255,47,176;
  --good:#5cf29a;
  --good-rgb:92,242,154;
  --bad:#ff5566;
  --warn:#ffd93d;
}
html,body{
  min-height:100%;
  background:
    radial-gradient(circle at 12% 12%,rgba(255,47,176,.25),transparent 34%),
    radial-gradient(circle at 88% 22%,rgba(47,230,255,.22),transparent 36%),
    radial-gradient(circle at 50% 92%,rgba(139,92,246,.24),transparent 42%),
    var(--bg)!important;
  background-attachment:fixed!important;
}
body:before,body:after{
  content:"";
  position:fixed;
  z-index:0;
  width:44vw;
  height:44vw;
  border-radius:50%;
  filter:blur(75px);
  opacity:.22;
  pointer-events:none;
  animation:drift 10s ease-in-out infinite alternate;
}
body:before{background:#ff2fb0;left:-18vw;top:8vh;}
body:after{background:#2fe6ff;right:-20vw;bottom:3vh;animation-delay:-5s;}
@keyframes drift{to{transform:translate3d(8vw,8vh,0) scale(1.18);}}
tw-story{
  position:relative;
  z-index:1;
  min-height:100vh;
  background:linear-gradient(180deg,rgba(10,11,16,.72),rgba(20,22,31,.88));
  backdrop-filter:blur(14px);
  color:var(--ink);
  font-family:'Segoe UI',system-ui,-apple-system,Roboto,sans-serif;
  line-height:1.5;
  max-width:720px;
  margin:0 auto;
  padding:0 0 3em 0;
}
tw-sidebar{display:none;}
tw-passage{
  display:block;
  padding:clamp(1.2em,5vw,2.6em) clamp(1em,5vw,2.4em) 4em;
  animation:passageIn .55s cubic-bezier(.2,.8,.2,1);
}
@keyframes passageIn{from{opacity:0;transform:translateY(18px) scale(.985);filter:blur(5px);}to{opacity:1;transform:none;filter:none;}}
tw-passage h1,tw-passage h2,tw-passage h3{
  color:var(--accent);
  text-shadow:0 0 14px rgba(var(--accent-rgb),0.55);
  letter-spacing:.045em;
  line-height:1.25;
  margin:.2em 0 .5em 0;
}
tw-passage h1{font-size:clamp(1.65em,7vw,2.5em);text-transform:uppercase;}
tw-passage h2{font-size:1.2em;color:var(--accent2);text-shadow:0 0 12px rgba(var(--accent2-rgb),0.5);}
tw-passage p{
  margin:.75em 0;
  max-width:38em;
  animation:wordUp .45s ease-out both;
}
tw-passage p:nth-of-type(2){animation-delay:.06s;}
tw-passage p:nth-of-type(3){animation-delay:.12s;}
tw-passage p:nth-of-type(4){animation-delay:.18s;}
tw-passage p:nth-of-type(n+5){animation-delay:.24s;}
@keyframes wordUp{from{opacity:0;transform:translateY(9px);}to{opacity:1;transform:none;}}
tw-passage hr{
  border:none;
  height:1px;
  margin:1.4em 0;
  background:linear-gradient(90deg,transparent,var(--accent),transparent);
  opacity:.5;
}
tw-passage em{color:var(--muted);}
tw-passage strong{color:var(--ink);}
tw-link, a.link{
  display:block;
  box-sizing:border-box;
  width:100%;
  min-height:56px;
  padding:1em 1.1em;
  margin:.7em 0;
  border:1px solid rgba(var(--accent-rgb),.85);
  border-radius:18px;
  color:var(--ink)!important;
  background:linear-gradient(125deg,rgba(var(--accent-rgb),.28),rgba(var(--accent2-rgb),.15));
  text-decoration:none!important;
  font-weight:600;
  font-size:1rem;
  cursor:pointer;
  box-shadow:0 10px 28px rgba(0,0,0,.28),inset 0 1px 0 rgba(255,255,255,.09);
  transition:transform .16s ease,box-shadow .16s ease,background .16s ease;
  animation:buttonPop .4s cubic-bezier(.2,.9,.25,1.15) both;
}
tw-link:nth-of-type(2){animation-delay:.04s;}tw-link:nth-of-type(3){animation-delay:.08s;}tw-link:nth-of-type(4){animation-delay:.12s;}tw-link:nth-of-type(n+5){animation-delay:.16s;}
@keyframes buttonPop{from{opacity:0;transform:scale(.94) translateY(8px);}to{opacity:1;transform:none;}}
tw-link:hover, tw-link:active, tw-link:focus{
  background:linear-gradient(125deg,rgba(var(--accent-rgb),.5),rgba(var(--accent2-rgb),.3));
  box-shadow:0 0 24px rgba(var(--accent-rgb),.5),0 14px 30px rgba(0,0,0,.34);
  transform:translateY(-2px) scale(1.015);
}
tw-link:active{transform:scale(.97);}
tw-broken-link{
  display:block;
  padding:.85em 1em;
  margin:.5em 0;
  border:1.5px dashed var(--bad);
  border-radius:12px;
  color:var(--bad)!important;
}
tw-passage textarea, tw-passage input[type=text]{
  display:block;
  width:100%;
  box-sizing:border-box;
  background:var(--bg3);
  color:var(--ink);
  border:1.5px solid var(--muted);
  border-radius:10px;
  padding:.7em .8em;
  font-size:1rem;
  font-family:inherit;
  margin:.4em 0 1em 0;
}
tw-passage textarea:focus, tw-passage input[type=text]:focus{
  outline:none;
  border-color:var(--accent);
  box-shadow:0 0 10px rgba(var(--accent-rgb),0.4);
}
.tag{
  display:inline-block;
  font-size:.72em;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:var(--bg);
  background:var(--accent);
  padding:.25em .6em;
  border-radius:999px;
  margin-bottom:.6em;
  font-weight:700;
}
.pron{color:var(--accent2);font-style:italic;margin:.1em 0 .6em 0;}
.card{
  background:linear-gradient(145deg,rgba(var(--accent-rgb),.14),rgba(20,22,31,.78));
  border:1px solid rgba(var(--accent-rgb),.55);
  border-radius:22px;
  padding:1.15em 1.2em;
  margin:1em 0;
  box-shadow:0 14px 36px rgba(0,0,0,.32),inset 0 1px 0 rgba(255,255,255,.08);
  animation:cardFloat 3s ease-in-out infinite alternate;
}
@keyframes cardFloat{to{transform:translateY(-4px);box-shadow:0 18px 42px rgba(0,0,0,.38),0 0 20px rgba(var(--accent-rgb),.12);}}
.vs{
  text-align:center;
  font-weight:800;
  color:var(--warn);
  margin:.6em 0;
  letter-spacing:.08em;
}
.stage-namepit{--accent:#ff2fb0;--accent-rgb:255,47,176;--accent2:#8b5cf6;--accent2-rgb:139,92,246;}
.stage-knockout{--accent:#ffd93d;--accent-rgb:255,217,61;--accent2:#ff2fb0;--accent2-rgb:255,47,176;}
.stage-finalvote{--accent:#5cf29a;--accent-rgb:92,242,154;--accent2:#2fe6ff;--accent2-rgb:47,230,255;}
.stage-result{--accent:#5cf29a;--accent-rgb:92,242,154;--accent2:#ffd93d;--accent2-rgb:255,217,61;animation:resultGlow 1.8s ease-in-out infinite alternate;}
@keyframes resultGlow{to{filter:drop-shadow(0 0 16px rgba(var(--good-rgb),.22));}}
.copybtn{
  display:block;
  width:100%;
  min-height:48px;
  padding:.85em 1em;
  margin:.6em 0;
  border:1.5px solid var(--good);
  border-radius:12px;
  background:rgba(var(--good-rgb),0.12);
  color:var(--good);
  font-weight:700;
  font-size:1rem;
  font-family:inherit;
  cursor:pointer;
}
.copybtn:active{background:rgba(var(--good-rgb),0.25);}
.resultbox{
  width:100%;
  box-sizing:border-box;
  min-height:7em;
  background:var(--bg3);
  color:var(--ink);
  border:1.5px solid var(--good);
  border-radius:10px;
  padding:.8em;
  font-family:inherit;
  font-size:.95rem;
}
@media (max-width:420px){
  tw-passage{padding:1.25em .9em 3em;}
  tw-passage h1{font-size:1.7em;}
  tw-story{line-height:1.42;}
  tw-link,a.link{font-size:.96rem;}
}
@media (prefers-reduced-motion:reduce){*,*:before,*:after{animation:none!important;transition:none!important;scroll-behavior:auto!important;}}

/* Game layer */
#gameHud{
  position:fixed;z-index:50;left:50%;top:max(10px,env(safe-area-inset-top));
  width:min(680px,calc(100% - 24px));transform:translateX(-50%);
  padding:10px 13px;border:1px solid rgba(255,255,255,.14);border-radius:15px;
  background:rgba(10,11,16,.82);backdrop-filter:blur(16px);box-shadow:0 10px 30px rgba(0,0,0,.32);
  font:800 11px/1.1 system-ui,sans-serif;letter-spacing:.12em;color:var(--ink);
}
.hud-top{display:flex;justify-content:space-between;gap:12px;margin-bottom:8px;}
#hudStage{color:#2fe6ff;text-shadow:0 0 9px rgba(47,230,255,.7);}
#hudCount{color:#ffd93d;}
.hud-track{height:5px;border-radius:99px;background:rgba(255,255,255,.1);overflow:hidden;}
#hudFill{display:block;height:100%;width:3%;border-radius:inherit;background:linear-gradient(90deg,#ff2fb0,#8b5cf6,#2fe6ff,#ffd93d);box-shadow:0 0 12px #2fe6ff;transition:width .65s cubic-bezier(.2,.8,.2,1);}
tw-story{padding-top:62px;}
#stageSplash{
  position:fixed;z-index:100;inset:0;display:grid;place-items:center;padding:28px;text-align:center;
  pointer-events:none;opacity:0;transform:scale(1.18);background:rgba(4,4,10,.86);
  color:white;font:950 clamp(2rem,10vw,4.7rem)/1.03 system-ui,sans-serif;letter-spacing:.035em;text-transform:uppercase;
  text-shadow:0 0 18px #ff2fb0,0 0 38px #2fe6ff;
}
#stageSplash.round-drop{animation:splashRound 2.1s cubic-bezier(.2,.8,.2,1) both;}
#stageSplash.title-drop{animation:titleCurtain 3.6s cubic-bezier(.18,.82,.2,1) both;}
@keyframes splashRound{0%{opacity:0;transform:scale(1.35) rotate(-3deg);}14%,78%{opacity:1;transform:scale(1) rotate(0);}100%{opacity:0;transform:scale(.86);}}
@keyframes titleCurtain{0%{opacity:0;background:#030307;}8%{opacity:1;}88%{opacity:1;}100%{opacity:0;}}
.drop-title{display:flex;flex-direction:column;align-items:center;gap:.08em;perspective:800px;}
.drop-title strong,.drop-title b{display:block;}
.drop-title strong{color:#f4ecdd;animation:titleDrop .9s cubic-bezier(.17,.86,.2,1.18) .2s both;}
.drop-title b{color:#ff2fb0;font-size:.82em;text-shadow:0 0 18px #ff2fb0,0 0 42px #8b5cf6;animation:titleDrop .9s cubic-bezier(.17,.86,.2,1.18) .72s both;}
.drop-title small{margin-top:1.5em;padding:.65em 1em;border:1px solid #2fe6ff;border-radius:99px;color:#2fe6ff;font:800 clamp(.58rem,2.5vw,.78rem)/1 system-ui,sans-serif;letter-spacing:.16em;text-shadow:0 0 10px #2fe6ff;animation:fileStamp .45s ease-out 1.55s both;}
@keyframes titleDrop{0%{opacity:0;transform:translateY(-75vh) rotate(-8deg) scale(1.22);}68%{opacity:1;transform:translateY(18px) rotate(1deg) scale(.98);}82%{transform:translateY(-8px) rotate(-.5deg);}100%{opacity:1;transform:none;}}
@keyframes fileStamp{0%{opacity:0;transform:scale(2.2) rotate(-8deg);}100%{opacity:1;transform:scale(1) rotate(-2deg);}}
.round-title{animation:roundImpact .55s cubic-bezier(.2,.9,.2,1.2) .15s both;}
@keyframes roundImpact{from{opacity:0;transform:scale(2.4) rotate(-5deg);filter:blur(8px);}to{opacity:1;transform:none;filter:none;}}
.case-core{
  position:relative;width:min(280px,82vw);aspect-ratio:1.22;margin:.25em auto 1.7em;display:flex;flex-direction:column;align-items:center;justify-content:center;
  border:1px solid rgba(var(--accent-rgb),.5);border-radius:26px;background:linear-gradient(145deg,rgba(255,255,255,.06),rgba(0,0,0,.28));
  box-shadow:inset 0 0 38px rgba(var(--accent-rgb),.09),0 16px 42px rgba(0,0,0,.3);overflow:hidden;
}
.case-core:before{content:"";position:absolute;inset:-80%;background:conic-gradient(transparent,rgba(var(--accent-rgb),.24),transparent 24%);animation:fileScan 7s linear infinite;}
.case-core>*{position:relative;z-index:1;}
.case-core>small{font:850 .63rem/1 system-ui,sans-serif;letter-spacing:.2em;color:var(--accent);}
.case-mark{width:115px;height:74px;margin:.7em 0 .5em;display:grid;place-items:center;border:3px double var(--accent2);transform:rotate(-3deg);color:var(--accent2);text-shadow:0 0 10px rgba(var(--accent2-rgb),.65);}
.case-mark span{font:900 .62rem/1 system-ui,sans-serif;letter-spacing:.28em;}
.case-mark b{font:1000 2rem/.9 system-ui,sans-serif;letter-spacing:.06em;}
.case-core>em{font:900 .66rem/1.2 system-ui,sans-serif;letter-spacing:.12em;color:var(--warn);font-style:normal;}
.case-core>i{margin-top:.75em;color:var(--muted);font:650 .49rem/1 system-ui,sans-serif;letter-spacing:.13em;font-style:normal;}
.opening-core{width:min(330px,88vw);box-shadow:inset 0 0 50px rgba(255,47,176,.1),0 0 34px rgba(47,230,255,.12),0 20px 48px rgba(0,0,0,.38);}
@keyframes fileScan{to{transform:rotate(360deg);}}
.beat-hidden{display:none!important;}
.beat-next{
  display:block;width:100%;min-height:58px;margin:1.2em 0 0;padding:1em;border:0;border-radius:18px;
  background:linear-gradient(120deg,#ff2fb0,#8b5cf6,#2fe6ff);background-size:200% 200%;
  color:white;font:900 1rem system-ui,sans-serif;letter-spacing:.13em;box-shadow:0 12px 30px rgba(139,92,246,.35);
  animation:nextPulse 1.5s ease-in-out infinite alternate;
}
@keyframes nextPulse{to{background-position:100% 50%;transform:translateY(-2px);box-shadow:0 16px 36px rgba(47,230,255,.35);}}
.choice-locked{display:none!important;}
.choice-hit{animation:choiceHit .22s ease-out!important;}
@keyframes choiceHit{50%{transform:scale(.93);filter:brightness(1.8);}100%{transform:scale(1.06);}}
.screen-hit tw-story{animation:screenHit .22s ease-out;}
@keyframes screenHit{25%{transform:translateX(-3px) rotate(-.15deg);}55%{transform:translateX(3px) rotate(.15deg);}100%{transform:none;}}
.choice-spark{position:fixed;z-index:120;pointer-events:none;color:#ffd93d;font-size:22px;text-shadow:0 0 10px #ff2fb0;animation:sparkFly .68s ease-out forwards;}
@keyframes sparkFly{to{opacity:0;transform:translate(var(--dx),var(--dy)) rotate(240deg) scale(.3);}}
.confetti{position:fixed;z-index:110;top:-25px;width:10px;height:18px;border-radius:3px;pointer-events:none;animation:confettiFall 2.5s linear forwards;}
@keyframes confettiFall{to{transform:translate3d(10vw,112vh,0) rotate(900deg);}}
@media (max-width:420px){#gameHud{width:calc(100% - 18px);padding:9px 11px;}tw-story{padding-top:58px;}}
